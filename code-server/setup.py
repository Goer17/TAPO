from setuptools import setup, find_packages

setup(
    name='code_server',
    version='1.0.0',
    description='A remote Python code interpretor server.',
    packages=find_packages(),
    install_requires=[],
)